# MiaoEyes 环境变量配置示例

# 服务器端口
PORT=3000

# JWT 密钥，用于生成验证令牌
JWT_SECRET=your_secure_secret_key_here

# 环境模式（development/production）
NODE_ENV=development

# 日志级别（debug/info/warn/error）
LOG_LEVEL=info

# 可选：Redis配置（用于分布式部署）
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_PASSWORD=your_redis_password

# 可选：数据库配置
# DB_HOST=localhost
# DB_PORT=3306
# DB_USER=root
# DB_PASSWORD=your_db_password
# DB_NAME=miaoeyes

# 域名跳转配置
# 是否启用域名跳转功能
# DOMAIN_REDIRECT_ENABLED=true

# 目标域名，验证成功后跳转到该域名
# TARGET_DOMAIN=your-target-domain.com

# 需要进行验证的域名列表，用逗号分隔
# REDIRECT_DOMAINS=verify.your-domain.com,auth.your-domain.com